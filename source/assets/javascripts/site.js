$(document).ready(() => {
  const $body = $('body');
  const $bodyId = $body.attr('id');

  switch ($bodyId) {
    case 'index':
      break;
    case 'four-o-four':
      break;
    default:
      break;
  }
});
